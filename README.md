# Hello World Test 1 — Lịch sử giao dịch (Tìm kiếm)

Expo app dựng từ Figma **Kết quả tính năng tìm kiếm** (`node-id=10132-53944`) với pattern MoMo UI Kit.

## Màn hình

1. **Gợi ý + lịch sử tìm kiếm**
2. **Kết quả tìm kiếm**
3. **Empty state**

Gõ `nạp` rồi Enter để xem kết quả; thử `xyz` để xem empty.

## Chạy

```bash
npm install
npx expo start
```

Branch: `Branch-1` — `QuocBinh-Do/Hello-World-Test-1`
